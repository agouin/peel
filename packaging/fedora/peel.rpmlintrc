# Whitelist spellcheck false positives for domain terms in the
# Summary / %description (shipped as Source1, read by rpmlint).
addFilter(r"^peel(\.src|\.[^:]+): E: spelling-error \('(resumable|zst|xz|gz|rar)',")
